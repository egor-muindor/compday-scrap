[project]
name = "compday-scrap"
version = "0.1.0"
description = "Scrapper for compday.ru"
requires-python = ">=3.14"
dependencies = ["playwright>=1.50"]

[tool.setuptools.packages.find]
include = ["scraper*"]
